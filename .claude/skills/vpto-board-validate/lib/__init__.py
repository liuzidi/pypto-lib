"""Vendored + path-neutralized VPTO onboard test-scaffold generators.

- `pto_parse`: parse .pto signatures, type mapping, golden-lib metadata.
- `setup_vpto`: generate launch.cpp + run.sh; the proven ptoas flags.
- `setup_main`: render the onboard main.cpp host program.
"""
